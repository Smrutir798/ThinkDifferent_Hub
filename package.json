{
  "name": "thinkdifferent-os",
  "version": "1.0.0",
  "description": "Developer-grade operating system dashboard for ThinkDifferent",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "npx concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "node server/dist/index.js",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
